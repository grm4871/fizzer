#!/usr/bin/env node
/** Thin executable entry point; command workflows live in cascade-chat-commands.mjs. */
import { main } from './cascade-chat-commands.mjs';

main().catch((err) => {
  process.stderr.write('cascade-chat: ' + (err instanceof Error ? err.message : String(err)) + '\n');
  process.exitCode = 1;
});
